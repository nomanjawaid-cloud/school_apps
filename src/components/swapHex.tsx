// const swapHexPositions = (fromIndex: number, toIndex: number) => {
//   // Do not allow center hex (index 0) to be moved
//   if (fromIndex === 0 || toIndex === 0) return;

//   setPositionsState((prev) => {
//     const newPositions = [...prev];
//     const temp = newPositions[fromIndex];
//     newPositions[fromIndex] = newPositions[toIndex];
//     newPositions[toIndex] = temp;
//     return newPositions;
//   });
// };
